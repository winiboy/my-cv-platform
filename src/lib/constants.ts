/**
 * Application-wide constants for the CV platform.
 */

/**
 * Quality threshold for CV evaluation (0-100 scale).
 *
 * A CV scoring at or above this threshold is considered acceptable
 * for job application purposes. CVs below this threshold should be
 * flagged for improvement before submission.
 *
 * @default 70
 */
export const QUALITY_THRESHOLD: number = 70;
