# Ralph progress — Milestone C Part 3: parity defects
# Contract: tasks/ralph/prd.json (branch ralph/milestone-c-part-3-parity-defects)
# PRD: tasks/prds/milestone-c-part-3-parity-defects.md (APPROVED 2026-09-15)

## US-001: The parity check names every Part 3 defect
Status: PASS
Priority: 1
Branch: ralph/milestone-c-part-3-parity-defects
Baseline HEAD: 64829aebbef2ef14f88737524ed761be820299e0
Worktree: E:/website/cv-website/my-cv-platform-part3 (the main checkout was in use by another session)

Scope:
- Scope check: PASS
- Changed paths: e2e/parity/verdicts.ts, e2e/parity/profiles.ts, e2e/parity/surfaces.ts,
  e2e/parity/colour.ts, e2e/parity/parity.spec.ts, e2e/parity/sidebar-column.spec.ts (new),
  e2e/fixtures/resume.ts (additive optional overrides; existing callers byte-neutral),
  tasks/ralph/prd.json (US-001 passes), tasks/ralph/progress.txt (this file)
- src/ untouched; e2e/visual untouched; no version change; no push or merge

Checks (run in the worktree on the final diff):
- pnpm typecheck: PASS — exit 0
- pnpm lint: PASS — 306 problems (236 errors, 70 warnings), equal to the origin/main baseline; none in e2e/parity or e2e/fixtures
- pnpm test (unit): PASS — 302/302
- pnpm test:integration: PASS — 27/27
- pnpm test:e2e: PASS — 55 passed; --list 55, no parity spec collected
- pnpm test:visual: PASS — 10 passed; --list 10; e2e/visual status empty
- pnpm build:verify: PASS — exit 0
- pnpm test:parity: PASS — two consecutive runs, 236 passed, exit 0 each; reports byte-identical,
  sha256 6df8b2f9197ec3634893126f1a16e53588bebac6e4336a9537448b998bad5caa

Specialized validation:
- Browser: NOT_APPLICABLE — no UI change
- Visual: PASS — baselines unchanged, visual suite green
- Export: NOT_APPLICABLE — no export code change; exported bytes measured by the parity check are unchanged
- Database: NOT_APPLICABLE
- Security: NOT_APPLICABLE — test harness only, no trust boundary touched

Agents:
- senior-coder: implementation, then one scoped fix round after review FAIL
- ui-expert: NOT_APPLICABLE — no rendered UI change
- code-reviewer: round 1 FAIL (signatures accepted unregistered divergences: US-011 DOCX size unpinned,
  US-012 declared-not-drawn face, not-chosen rows without reference, US-003/US-013 Preview side unpinned,
  US-013 print-band FINDING incomplete); round 2 PASS — reviewer's own offline harness applied 17
  perturbations (11 new shapes) to the round-2 observations, every one NEW

Acceptance criteria:
- AC-01: PASS — KnownId union in e2e/parity/verdicts.ts uses Part 3 story ids (US-002-sections-classic ... US-015-creative-sections); P3-US00N ids gone
- AC-02: PASS — independent recount: all 105 non-MATCH rows of report 10fe632c map to the Divergence Inventory story; all 82 MATCH rows unchanged; F-A row is KNOWN US-013-accent-hue
- AC-03: PASS — multi-page profile prints >= 2 pages (asserted in parity.spec.ts); colour:print-sidebar-column is measured (NOT EXERCISED 0)
- AC-04: PASS — font-not-chosen profile (stored default) and chosen-font rows both measured; minimal/creative not-chosen rows referenced to Inter
- AC-05: PASS — report: MATCH 89, KNOWN 114, NEW 0, unreconciled 0; all 16 known ids CONFIRMED; 25 perturbation cases turn NEW (perturb-check log)
- AC-06: PASS — git diff -- src empty; e2e/visual unchanged
- PRD-wide items (FR, regression, verification, FAIL, BLOCKER, out of scope): PASS — no product behaviour changed, required checks above green, no FAIL/BLOCKER condition met

Open owner decisions recorded by this story (not blocking US-001):
- Before US-012: which family classic body text uses when no font was chosen. Classic draws Inter
  today, so "classic stays serif" and "no unchosen font changes" cannot both hold. Also confirm that
  professional/modern not-chosen rows keep the stored default (Arial), as the parity reference now assumes.
- Before US-013: acceptance criterion 2 (multi-page print shows a visible band) cannot be exercised as
  written — under print html/body are white !important and the band gradient ends where body ends, so
  colour:print-sidebar-column matches whether or not the band follows the model. Revise the criterion.

Environment notes:
- Round 1 saw three ~15 s DOCX stalls, all on the first DOCX request after server start
  (collect · primary · professional); re-run, not absorbed. Round 2 had no stall.
- The parity report is deterministic per machine (font-face resolution depends on installed fonts).

Commit:
- Story commit: THIS_COMMIT
- Commit message: ralph: US-001 the parity check names every Part 3 defect

FINAL: PASS

## US-002: The classic and minimal DOCX stop dropping sections
Status: PASS
Priority: 2
Branch: ralph/milestone-c-part-3-parity-defects
Baseline HEAD: ac21dcc (tree identical to origin/main 3e32910)
Worktree: E:/website/cv-website/my-cv-platform-part3

Scope:
- Scope check: PASS
- Changed paths: src/lib/layout-settings.ts, src/lib/layout-settings.test.ts,
  src/app/api/resumes/[id]/download-docx/docx-classic.ts, docx-minimal.ts (comment-only),
  src/app/api/resumes/[id]/download-docx/docx-single-column-sections.test.ts (new),
  e2e/docx-classic-layout.spec.ts, e2e/docx-minimal-layout.spec.ts,
  e2e/parity/profiles.ts, e2e/parity/verdicts.ts,
  tasks/ralph/prd.json, tasks/ralph/progress.txt
- No Preview/template source changed; no visual baseline moved; no version change

Decision (AC-03) — the filtering changed, not the vocabulary:
  mapEditorOrderToSingleColumn took section membership from the editor's main order, which
  parseLayoutModel filters to VALID_MAIN_IDS, so skills and projects never reached the dispatch.
  Membership now comes from a per-template sequence (CLASSIC_SEQUENCE, MINIMAL_SEQUENCE); the
  stored order decides only the positions it can name. VALID_MAIN_IDS, parseLayoutModel and
  DEFAULT_RESUME_LAYOUT are byte-unchanged, so professional, modern and creative cannot move.
  Widening the vocabulary was rejected: the editor offers neither id on any template, the list is
  also the other three templates' vocabulary and the source of the default order (FR-5), and
  skills is already a sidebar id. Recorded in the DECISION (Part 3 US-002) comment in
  src/lib/layout-settings.ts.

Checks (re-run on the final diff):
- pnpm typecheck: PASS — exit 0
- pnpm lint: PASS — 306 problems, at baseline, under the PRD's 311 ceiling
- pnpm test (unit): PASS — 333/333, including 4 new empty-category cases
- pnpm test:integration: PASS — 27/27
- pnpm test:e2e: PASS — 55 passed; --list 55
- pnpm test:visual: PASS — 10 passed; --list 10; e2e/visual status empty
- pnpm build:verify: PASS — exit 0
- pnpm test:parity: PASS — two runs, 236 passed each, reports byte-identical,
  sha256 e8a314cdc086632a603152560d680e81c1b488e2e4dbcfc6b2b92585b7e37eda
  Counts: MATCH 93, KNOWN 110, NEW 0, unreconciled 0, FINDING 9, DECISION 3

Specialized validation:
- Browser: NOT_APPLICABLE — no UI change
- Visual: PASS — baselines unchanged, suite green
- Export: PASS — generated DOCX inspected as unzipped word/document.xml, not an HTTP 200;
  artifact comparison over two layouts: professional/modern/creative byte-identical before and
  after (6 hashes), classic and minimal insertions-only
- Database: NOT_APPLICABLE
- Security: NOT_APPLICABLE — no trust boundary touched; stored layout stays untrusted and bounded

Agents:
- ui-expert: NOT_APPLICABLE — no rendered UI change
- code-reviewer: round 1 FAIL — classic dropped a visible skill category with no items (heading
  above nothing, disagreeing with its own Preview and with minimal); plus two behaviour changes
  unrecorded. Round 2 PASS, with the reviewer reproducing red-before against HEAD's generator and
  running its own probe suites.

Acceptance criteria:
- AC-01: PASS — docx-single-column-sections.test.ts unzips word/document.xml and asserts both
  sections; e2e/docx-classic-layout.spec.ts covers the real route
- AC-02: PASS — same coverage for minimal
- AC-03: PASS — see Decision above
- AC-04: PASS — artifact comparison, 6 byte-identical hashes across two layouts; the changed
  exports have no consumer outside the two single-column generators
- AC-05: PASS — no template source in the diff; visual 10/10 with no snapshot update
- AC-06: PASS — hidden main sections, hidden categories, hidden projects and all-items-hidden
  (heading included) all stay out, verified by the reviewer's independent probes
- AC-07: PASS — the four visibility rows for classic and minimal are MATCH; both
  US-002-sections-* ids removed from the known list (FR-10)
- PRD-wide items: PASS — AC-48 holds (no section list in any generator), FR-2/FR-5 hold,
  lint within ceiling, no tolerance or timeout widened

Findings recorded by this story (annotations in the parity report, not defects of it):
- hiddenSidebarSections: skills is a no-op on every classic/minimal surface. Before this story the
  DOCX appeared to honour it; now it does not. Not gating the new sections on a list no Preview
  reads follows FR-3. US-014 owns the control gap under FR-9; the stored value is preserved.
- Project descriptions (and a category's skillsHtml) are stored as HTML: the classic and minimal
  Previews render them plainly while the generators parse them. The DOCX is right and the Preview
  is wrong. No Part 3 story owns Preview rich-text rendering — an owner decision to place it.

Open reviewer finding carried to US-009:
- The section order is hand-transcribed in three places (CLASSIC_SEQUENCE/MINIMAL_SEQUENCE,
  PREVIEW_ORDER, the JSX) with no guard: reordering a template's JSX breaks nothing today. No
  parity profile uses the default main order, so those rows are permanently KNOWN US-009. A
  default-order profile scoped to ['classic','minimal'] closes it cheaply in US-009.

Commit:
- Story commit: THIS_COMMIT
- Commit message: ralph: US-002 the classic and minimal DOCX stop dropping sections

FINAL: PASS

## US-003: DOCX text colour matches the palette the Preview renders
Status: PASS
Priority: 3
Branch: ralph/milestone-c-part-3-parity-defects
Baseline HEAD: 23af3af (tree identical to origin/main 3e84df6); PRD amendments d9f7228, ef1993c
Checkout: E:/website/cv-website/my-cv-platform (main checkout; no worktree)

Scope:
- Scope check: PASS
- Changed paths: src/lib/resume-palette.ts (new), src/lib/resume-palette.test.ts (new),
  src/app/api/resumes/[id]/download-docx/docx-palette.ts (new), docx-palette.test.ts (new),
  docx-text-palette.test.ts (new), docx-{professional,modern,classic,minimal,creative}.ts,
  docx-helpers.ts, docx-helpers.test.ts, e2e/parity/verdicts.ts,
  tasks/ralph/prd.json, tasks/ralph/progress.txt
- No template, globals.css or visual-baseline change; no version change
- Widened on purpose from title/heading/body to every solid text-run colour: criterion 2 forbids a
  second hand-copied table, and a partial table left classic's title and job titles in different
  colours although the Preview draws both in slate-900

Decision (AC-02) — how the DOCX obtains the Preview's palette:
  src/lib/resume-palette.ts holds each template's colours in the form the Preview source writes them
  (oklch(...), #374151), keyed by the Preview's own name for the colour (a token such as 'slate-900'
  or an inline role). docx-palette.ts converts them to DOCX hex once at module load and throws on
  anything it cannot convert. resume-palette.test.ts guards both directions: each entry must equal
  its globals.css @theme value (or Tailwind's default theme for tokens globals.css leaves alone) and
  appear in its template; and every colour a template draws, with its count, must resolve to a
  palette entry or to an exclusion naming its owner. The generators never import e2e/parity, so
  parity still compares the DOCX against what the browser draws. Rejected: reading globals.css at
  request time (depends on deployment layout) and importing CSS as text (no bundler rule).
  Known limit: the completeness check cannot see which element a colour sits on, so swapping two
  elements' colours without changing the counts passes it; parity and the per-element DOCX tests
  cover the sampled elements. Non-literal values under non-colour style keys and attributes are not
  scanned; none is a text colour and no template uses one.

Checks (on the final diff):
- pnpm typecheck: PASS — exit 0
- pnpm lint: PASS — 305 problems, under the 311 ceiling; new and changed files clean
- pnpm test (unit): PASS — 765/765
- pnpm test:integration: PASS — 27/27
- pnpm test:e2e: PASS — 116 passed; --list 116
- pnpm test:visual: PASS — 10 passed; --list 10; e2e/visual status empty
- pnpm build:verify: PASS — exit 0
- pnpm test:parity: PASS — two runs, 236 passed each, reports byte-identical,
  sha256 efec255ed33b80774597910a7bc25e7d7a10d7d0290827afe3c67de1eaf7dcd2 (reproduced by the reviewer)
  Counts: MATCH 106, KNOWN 97, NEW 0, unreconciled 0; 13 rows KNOWN US-003-palette -> MATCH

Specialized validation:
- Browser: NOT_APPLICABLE — no UI change
- Visual: PASS — baselines unchanged
- Export: PASS — artifact comparison, 2 layouts x 5 templates: document.xml byte-identical once run
  colours, fills and borders are masked; only the listed colour changes appear; user sidebar and
  accent colours unchanged in every fill and run
- Database: NOT_APPLICABLE
- Security: NOT_APPLICABLE

Agents:
- ui-expert: NOT_APPLICABLE — no rendered UI change
- code-reviewer: round 1 BLOCKER — three divergences the PRD did not name (professional opacity-80
  sidebar text, creative translucent header text, stock border and fill colours), resolved by PRD
  amendment d9f7228; round 2 FAIL — the completeness check failed open for arbitrary-value classes and
  non-inline style objects; round 3 PASS, with 18 mutations run by the reviewer

Acceptance criteria:
- AC-01: PASS — docx-text-palette.test.ts pins every moved element to the palette (123 of 155 red
  against HEAD's generators); parity's 13 sampled rows MATCH
- AC-02: PASS — see Decision
- AC-03: PASS — classic and minimal rules, creative header fill (first stop 693AD4, gradient recorded
  as a LIMITATION) and badge fill, professional underline, all from the palette
- AC-04: PASS — the completeness check catches every text-colour mutation tried, 11 by the implementer
  and 18 by the reviewer
- AC-05: PASS — user colours byte-identical in the artifact comparison
- AC-06: PASS — no Preview source changed; visual 10/10, no snapshot update
- AC-07: PASS — 13 rows MATCH; US-003-palette removed from the known list (FR-10)
- PRD-wide items: PASS — no NEW row, no tolerance widened, no other template output changed beyond
  run, fill and border colours

Divergences found and placed by amendment (not defects of this story):
- d9f7228: professional opacity-80 sidebar text and creative translucent header text -> US-006;
  stock borders and fills -> US-003
- ef1993c: modern technology chip fill; creative timeline line and dots, project card rule and fill,
  decorative header circles -> US-007

Commit:
- Story commit: THIS_COMMIT
- Commit message: ralph: US-003 DOCX colours come from the palette the Preview renders

FINAL: PASS

## US-004: DOCX line spacing matches the leading the Preview draws
Status: PASS
Priority: 4
Branch: ralph/milestone-c-part-3-parity-defects
Baseline HEAD: 80eab66 (PRD amendments b1721f3 line-height scope, 80eab66 US-016 added)
Checkout: E:/website/cv-website/my-cv-platform (main checkout; no worktree)

Scope:
- Scope check: PASS
- Changed paths: src/lib/resume-line-height.ts (new), src/lib/resume-line-height.test.ts (new),
  src/app/api/resumes/[id]/download-docx/docx-line-spacing.test.ts (new),
  docx-{professional,modern,classic,minimal,creative}.ts, docx-helpers.ts, docx-helpers.test.ts,
  src/components/dashboard/resume-templates/{professional,modern}-template.tsx,
  src/lib/format-text.tsx, e2e/parity/{profiles,surfaces,verdicts}.ts,
  tasks/ralph/prd.json, tasks/ralph/progress.txt
- Template edits are pure refactors (constants imported from the shared module); no visual baseline
  moved; no version change

Decisions:
- Rule: exact everywhere. atLeast draws the font's own single line when that is taller: Verdana's
  1.215em exceeds the 1.2 ratio of the professional and modern titles, so those rows could never
  match. Word takes any shortfall from above the text; the tightest case is 1.2 with Verdana.
- One source: src/lib/resume-line-height.ts. Values TypeScript cannot import from CSS (preflight
  1.5, Tailwind text-* and leading-relaxed, .formatted-content 1.4) are declared in the form the CSS
  writes them, and resume-line-height.test.ts reads that CSS and the templates and fails on drift.
  rendersAsFormattedContent is shared by the Preview's renderFormattedText and the generators, so
  the two cannot disagree on what counts as HTML.
- Own padding goes in the line; a sibling's height goes in the gaps. Modern's job-title bar is a
  filled box whose padding Word shading must cover, so it is written into the line. Creative's
  heading bar is a separate flex child, so its surplus is written as space before and after, split
  as items-center does. Writing the bar into the line made the sectionHeading row NEW at x1.655
  against the Preview x1.500, because that row compares the heading text leading, not the row
  height; and a wrapped heading would have been spaced at the bar height.

Checks (on the final diff):
- pnpm typecheck: PASS - exit 0
- pnpm lint: PASS - 305 problems, unchanged, under the 311 ceiling
- pnpm test (unit): PASS - 991/991 across 21 files
- pnpm test:integration: PASS - 27/27
- pnpm test:e2e: PASS - 116 passed; --list 116
- pnpm test:visual: PASS - 10 passed; --list 10; e2e/visual status empty
- pnpm build:verify: PASS - exit 0
- pnpm test:parity: PASS - two runs, 240 passed each, reports byte-identical,
  sha256 52889fe7ac99780d64706d9a48629d83b9a609ee216e14c23ea6808a4604648a
  Counts: MATCH 123, KNOWN 82, NEW 0, unreconciled 0, LIMITATION 11, FINDING 9, DECISION 4

Specialized validation:
- Browser: NOT_APPLICABLE - no rendered UI change (template edits are constant imports)
- Visual: PASS - baselines unchanged
- Export: PASS - artifact comparison, 5 templates x 2 layouts: every zip part identical once
  w:line/w:lineRule and the listed gap changes are accounted for; page counts verified in Word 16
  across 50 generated documents
- Database: NOT_APPLICABLE
- Security: NOT_APPLICABLE - format-text.tsx keeps the same regex and the inert sanitiser

Agents:
- ui-expert: NOT_APPLICABLE - no rendered UI change
- code-reviewer: round 1 BLOCKER - creative heading bars were a divergence the PRD does not name,
  recorded only inside a FINDING; resolved inside the story under its own taller-box rule. Round 2
  PASS, the reviewer reproducing the creative arithmetic at five font scales, re-running minimal-06
  through Word (3 pages without the fix, 2 with) and running ten drift-guard mutations of its own

Acceptance criteria:
- AC-01: PASS - every site uses exactLineSpacing; no LineRuleType.AUTO remains; the list helpers
  require a line spacing so the compiler finds any caller that omits one
- AC-02: PASS - 152 paragraph sites, none without line spacing; each template document default is
  exact; the reviewer confirmed on its own richer fixture at three scales
- AC-03: PASS - formatted (HTML) body text writes 1.4, read from globals.css; the new formatted-body
  parity profile (professional and modern, the templates that render .formatted-content) MATCHes
- AC-04: PASS - the harness counts only exact / run size as drawn leading; atLeast is its own kind
  and an unknown rule throws; all 15 primary line-height rows MATCH
- AC-05: PASS - Word 16 to PDF to raster: accented capitals and descenders whole in Verdana at 1.2
  and Times at 1.5; drawn pitch equals the written value. Residual FINDING records Word placing the
  baseline at 80% of an exact line (up to ~0.1em), modern job-title baseline ~2.5pt low inside a bar
  of the Preview height, a wrapped creative heading ~8px taller than the Preview, and 1-twip rounding
- AC-06: PASS - visual 10/10, no snapshot update; template diffs are constant imports only
- AC-07: PASS - 15 rows KNOWN to MATCH plus 2 new formatted-body rows MATCH; US-004-line-spacing
  removed from the known list and the Word auto line spacing FINDING closed, replaced by a DECISION
- PRD-wide items: PASS - no NEW row, no tolerance widened (LINE_HEIGHT_TOLERANCE untouched), no
  other template affected

Known limits recorded:
- The drift guard cannot see an element remapped from one declared ratio to another; the sampled
  title, heading and body rows are pinned by parity and docx-line-spacing.test.ts, the remaining
  paragraph sites by review. Stated in the test header comment, following the US-003 precedent.
- Creative heading centring uses space-before, which Word can suppress at the top of a page under
  some compatibility settings.

Commit:
- Story commit: THIS_COMMIT
- Commit message: ralph: US-004 DOCX line spacing matches the leading the Preview draws

FINAL: PASS

## US-005: DOCX letter spacing matches the Preview
Status: PASS
Priority: 5
Branch: ralph/milestone-c-part-3-parity-defects
Baseline HEAD: dc0f33d
Checkout: E:/website/cv-website/my-cv-platform (main checkout; no worktree)

Scope:
- Scope check: PASS
- Changed paths: src/lib/resume-letter-spacing.ts (new), src/lib/resume-letter-spacing.test.ts (new),
  src/app/api/resumes/[id]/download-docx/docx-letter-spacing.test.ts (new), docx-helpers.ts,
  docx-{professional,modern,classic,minimal,creative}.ts,
  src/components/dashboard/resume-templates/modern-template.tsx,
  e2e/parity/{surfaces,verdicts}.ts, tasks/ralph/prd.json, tasks/ralph/progress.txt
- Modern's template change is a constant import rendering the identical string; no visual baseline
  moved; no version change

Decisions:
- One source: src/lib/resume-letter-spacing.ts, a sibling of resume-palette.ts (US-003) and
  resume-line-height.ts (US-004). It holds Tailwind's six --tracking-* values in em as theme.css
  writes them, plus a per-template record keyed by the Preview's own name for each element. Modern's
  template imports its four inline values. Conversion lives with the other DOCX unit maths in
  docx-helpers.ts: trackingSpacing(em, halfPoints) = round(em * halfPoints * 10), following the run
  size so font scale carries.
- Comparison basis changed from absolute twips to em. Preview and DOCX are anchored to different
  font sizes while US-011-font-size is open, so an em-correct DOCX would still have differed in
  absolute twips (modern's title 98 against 81) and four rows would have turned NEW - reporting the
  size divergence a second time under letter spacing. Each surface is now held to half of the
  unchanged TWIPS_TOLERANCE in its own encoding. The reviewer recomputed the slack per row and
  mutated encoded values: +/-2 twips turns every row NEW and +/-1 turns 9 of 15 NEW, where the old
  absolute rule accepted +/-1 on all of them, so this is a tightening. The size divergence remains
  independently KNOWN on the font-size rows.
- Scope covers every element whose characterSpacing the generators write, not only the sampled ones
  (modern's contact labels, skill categories and job-title bar; professional's headings and title).
  Criterion 1's first clause is unqualified, and it is the same named divergence on the same
  property; the reviewer accepted this rather than treating it as unnamed.

Checks (on the final diff):
- pnpm typecheck: PASS - exit 0
- pnpm lint: PASS - 305 problems, unchanged, under the 311 ceiling
- pnpm test (unit): PASS - 1031/1031 across 23 files
- pnpm test:integration: PASS - 27/27
- pnpm test:e2e: PASS - 116 passed; --list 116
- pnpm test:visual: PASS - 10 passed; --list 10; e2e/visual status empty
- pnpm build:verify: PASS - exit 0
- pnpm test:parity: PASS - two runs, 240 passed each, reports byte-identical,
  sha256 09e2c59b45eedee6cbf1a19ce9a1a9d936fc72c48dea8c95d232d4a0b71931fc
  Counts: MATCH 131, KNOWN 74, NEW 0, unreconciled 0

Specialized validation:
- Browser: NOT_APPLICABLE - no rendered UI change
- Visual: PASS - baselines unchanged
- Export: PASS - artifact comparison, 5 templates x 2 layouts: every changed line in all ten
  documents is a run-level w:spacing, zero of any other kind; all ten styles.xml byte-identical
- Database: NOT_APPLICABLE
- Security: NOT_APPLICABLE

Agents:
- ui-expert: NOT_APPLICABLE - no rendered UI change
- code-reviewer: PASS first round, having re-derived trackingSlack and equal from the harness and
  driven them with the measured sizes, verified the drift guard's Tailwind and preflight assertions
  on this machine, and re-run typecheck, unit, lint and the artifact comparison itself. One required
  change, a misplaced constant separating a comment from the constants it documents, was applied.

Acceptance criteria:
- AC-01: PASS - all 15 letter-spacing rows MATCH; docx-letter-spacing.test.ts pins every run in both
  directions (a claimed run carries exactly that spacing; an unclaimed run carries none)
- AC-02: PASS - every case runs at scales 0.8, 1 and 1.2; the artifact comparison shows layout B
  values differing from layout A throughout
- AC-03: PASS - visual 10/10, no snapshot update; the template edit renders the identical string
- AC-04: PASS - 8 rows KNOWN -> MATCH; US-005-letter-spacing removed from the known list (FR-10)
- PRD-wide items: PASS - no NEW row, no tolerance widened (TWIPS_TOLERANCE untouched at 1), no other
  template affected

Known limits recorded:
- Sub-twip quantisation: professional's heading needs 6.5 twips and is written as 7 (0.0269em against
  0.025em); modern's title 0.1508em; minimal's and creative's titles -0.0244em. Recorded in the
  DECISION annotation, following the precedent US-004 set for Word's exact spacing.
- surfaces.ts divides by halfPoints * 10 without a guard; safe today behind an existing null check,
  but a zero-size run would yield Infinity rather than a diagnosable failure. Left for a later
  harness story.

Commit:
- Story commit: THIS_COMMIT
- Commit message: ralph: US-005 DOCX letter spacing matches the Preview

FINAL: PASS

## US-006: Translucent text keeps its tint in the DOCX
Status: PASS
Priority: 6
Branch: ralph/milestone-c-part-3-parity-defects
Baseline HEAD: 9f9e2fe; story amended by d9f7228 (professional and creative added to US-006)
Checkout: E:/website/cv-website/my-cv-platform (main checkout; no worktree)

Scope:
- Scope check: PASS
- Changed paths: src/lib/resume-text-opacity.ts (new), src/lib/resume-text-opacity.test.ts (new),
  src/app/api/resumes/[id]/download-docx/docx-text-opacity.ts (new), docx-text-opacity.test.ts (new),
  docx-{professional,modern,creative}.ts, docx-helpers.ts, src/lib/resume-palette.test.ts,
  e2e/parity/{colour,profiles,surfaces,verdicts}.ts, tasks/ralph/prd.json, tasks/ralph/progress.txt
- No template file touched, so Preview rendering is unchanged by construction; no visual baseline
  moved; no version change

Decisions:
- One source: src/lib/resume-text-opacity.ts holds each template's per-element alpha in the form its
  template writes it, with classic and minimal carrying empty records as the statement that they
  draw none. docx-text-opacity.ts mirrors docx-palette.ts and takes its foreground from the palette.
- Compositing in gamma-encoded sRGB, source-over, alpha quantised to eight bits first, which makes
  the arithmetic byte-identical to compositeOver in e2e/parity/colour.ts so the two sides of a row
  cannot disagree over rounding. Measured on painted pixels, every Chromium path lands within one
  level and none matches exactly: an opacity layer and an rgba() fill both paint a level low
  (#CECEED against the written CFCFED at 0.8 over #0D0DA5), while color-mix(in oklab) is exact in
  one case and a level high elsewhere. Recorded in the module.
- Modern's template does not import the alphas, unlike US-004 and US-005. They sit inside complete
  rgba(255,255,255,0.6) literals that the palette guard scans as colours; interpolating a constant
  would turn five readable literals into expressions the scanner cannot read, weakening one guard to
  build another. The drift test replaces the coupling in both directions.
- TEXT_BACKDROP was demoted to a comment rather than consumed by the generators: a consumable
  version needs a role tag at every call site, still cannot check the hex the generator passes, and
  would change generator code for no new guarantee. The reviewer preferred this to its own suggestion.

Checks (on the final diff):
- pnpm typecheck: PASS - exit 0
- pnpm lint: PASS - 305 problems, unchanged, under the 311 ceiling
- pnpm test (unit): PASS - 1072/1072 across 25 files (1074 before TEXT_BACKDROP's two tests went
  with the constant)
- pnpm test:integration: PASS - 27/27
- pnpm test:e2e: PASS - 116 passed; --list 116
- pnpm test:visual: PASS - 10 passed; --list 10; e2e/visual status empty
- pnpm build:verify: PASS - exit 0
- pnpm test:parity: PASS - two runs, byte-identical,
  sha256 cd8c46123abea60e7583ae8fe4c80514cdc64deada28e031849097a0958357f9
  Counts: MATCH 136, KNOWN 72, NEW 0, unreconciled 0

Specialized validation:
- Browser: PASS - painted pixels read from the real creative Preview through the parity config, to
  establish what the header actually draws behind its translucent text
- Visual: PASS - baselines unchanged
- Export: PASS - artifact comparison, 5 templates x 2 layouts: classic and minimal byte-identical;
  the other six change only run colours, and masking every colour makes before and after identical
- Database: NOT_APPLICABLE
- Security: NOT_APPLICABLE

Agents:
- ui-expert: NOT_APPLICABLE - no rendered UI change
- code-reviewer: round 1 FAIL - the creative LIMITATION stated two things measurably false, the
  substituted backdrop printed like a measured one, and opacityOf contradicted its own comment.
  Round 2 PASS, the reviewer re-measuring all ten cited pixels in its own browser (10/10 within one
  level, four exact), confirming the overNote appears on those two rows and nowhere else, and
  re-running both guards and the parity mutation checks.

Acceptance criteria:
- AC-01: PASS - professional 8 sites, modern 5, creative 4 write the composited colour; all eight
  expected hexes recomputed by hand by the reviewer, independently of the code
- AC-02: PASS - unit tests at two sidebar colours with literal expected hexes, plus two more colours
  in the artifacts
- AC-03: PASS - three rows added (professional sidebarSecondary, creative headerSummary and
  headerLinks), all MATCH
- AC-04: PASS - the creative LIMITATION names the top-left corner as the only place the fill is
  right, both discs with centres and radii, the four measured backdrop hexes, the Preview tints
  #F8EFF9 and #FADCEE against the DOCX's #F0ECFB and #E1D8F6, and the 25-level red gap as a hue
  difference rather than a lightening
- AC-05: PASS - no template file changed; visual 10/10
- AC-06: PASS - modern's two rows KNOWN -> MATCH; US-006-translucent-text removed from the known
  list (FR-10)
- PRD-wide items: PASS - no NEW row, COLOUR_CHANNEL_TOLERANCE untouched at 1, classic and minimal
  byte-identical

Known limits recorded:
- The two creative rows composite both surfaces over the same substituted colour, the fill the DOCX
  draws, so they MATCH by construction. The report now prints that in the row itself, and
  seenColour's contract names the exception.
- Accepted without acting: the reviewer suggested noting that the two 50% corner hexes in the
  annotation are measured under a disc. True and harmless, since the next sentence names the discs;
  not worth two further parity runs for a clarifying clause.
- Removing an alpha entry whose written form another element of the same template shares does not
  fail the palette guard; docx-text-opacity.test.ts and typecheck both catch it. Documented in the
  guard's not-covered list.

Commit:
- Story commit: THIS_COMMIT
- Commit message: ralph: US-006 translucent text keeps its tint in the DOCX

FINAL: PASS
