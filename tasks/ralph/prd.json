{
  "project": "my-cv-platform",
  "branchName": "chore/rls-schema-hardening",
  "description": "Close the schema and policy defects confirmed by the Phase 26 empirical RLS audit in one forward migration, without changing any user-visible application behaviour.",
  "userStories": [
    {
      "id": "US-001",
      "title": "handle_new_user() resolves names deterministically",
      "description": "As the platform operator, I want the SECURITY DEFINER signup trigger to resolve unqualified names through a fixed search_path, so that a schema earlier in a caller's search path cannot influence what it executes.",
      "acceptanceCriteria": [
        "SELECT proconfig FROM pg_proc WHERE proname = 'handle_new_user' returns an array containing search_path=public, pg_temp, not null.",
        "The function remains SECURITY DEFINER (prosecdef is true).",
        "Inserting a row into auth.users still creates the matching public.profiles row with id, email, full_name and avatar_url populated as before.",
        "The migration replaces the function rather than dropping and recreating the on_auth_user_created trigger.",
        "FR-1: All changes land in one new forward migration. No already-applied migration file is edited.",
        "FR-2: The migration is idempotent where PostgreSQL permits it (CREATE OR REPLACE, DROP ... IF EXISTS, policy drop-then-create), so a re-run against an already-migrated database does not error.",
        "FR-3: The migration performs no destructive or lossy data change. No row is deleted and no column is dropped.",
        "FR-4: supabase/tests/rls-audit.sql is extended so that every acceptance criterion expressible as a SQL check is asserted there, and the suite passes in full.",
        "FR-5: RLS remains enabled on all eight tables and the policy count does not decrease.",
        "FR-6: No application source file changes, with the sole exception of documentation updates required by US-005.",
        "FR-7: The migration is applied and verified against the local stack only. Deploying it to any hosted environment is out of scope and requires separate explicit authorization.",
        "Regression constraint: Cross-user isolation must continue to hold on all eight tables: the 20 isolation checks in the audit harness must still pass unchanged.",
        "Regression constraint: An owner must retain SELECT, UPDATE and DELETE on their own rows wherever those policies exist today.",
        "Regression constraint: anon must continue to read 0 rows from profiles and cover_letters, and must continue to read a resume with is_public = true - this PRD does not change S3 behaviour in either direction.",
        "Regression constraint: The on_auth_user_created trigger must continue to create a profile row on signup.",
        "Regression constraint: updated_at maintenance must continue on every table that has it today.",
        "Regression constraint: pnpm build, pnpm typecheck and pnpm test must remain green, and pnpm lint must not increase above its 311-problem baseline.",
        "Verification: Migration applies cleanly - pnpm supabase db reset completes with all six migrations applied in order, from an empty database.",
        "Verification: Effective policy behaviour - the extended supabase/tests/rls-audit.sql runs against the local stack and reports every check PASS, with the new checks visible in the output. Evidence is the run output, not the migration source.",
        "Verification: Catalog state - pg_proc.proconfig for handle_new_user, pg_policy rows for resume_analyses and the five UPDATE policies, and pg_constraint for cv_generation_logs_user_id_fkey are queried directly and recorded.",
        "Verification: Idempotency - the migration is applied a second time against an already-migrated database and completes without error.",
        "Verification: Repository baseline - pnpm lint, pnpm typecheck, pnpm test and pnpm build are run and their results recorded.",
        "Verification: Documentation - docs/engineering/rls-audit.md is updated to reflect the post-migration state, including which findings are now closed.",
        "FAIL condition: Any existing check in supabase/tests/rls-audit.sql regresses from PASS.",
        "FAIL condition: Cross-user isolation weakens on any table.",
        "FAIL condition: handle_new_user loses SECURITY DEFINER, or signup stops creating a profile row.",
        "FAIL condition: The migration deletes rows, drops a column, or edits an already-applied migration file.",
        "FAIL condition: Application behaviour changes in any way observable to a user.",
        "FAIL condition: The S1 clauses are described anywhere in the commit, migration or docs as fixing a vulnerability.",
        "FAIL condition: PASS is claimed for any criterion without a recorded command and result.",
        "BLOCKER condition: cv_generation_logs contains rows whose user_id has no matching public.profiles row, making the US-004 foreign key unachievable without a data decision that this PRD does not authorize.",
        "BLOCKER condition: The local Supabase stack cannot be started, since every verification step depends on it.",
        "BLOCKER condition: Implementation is attempted on main, or on a branch other than the one approved for this work.",
        "Out of scope: The is_public column allow-list (finding S3). It is a product decision requiring its own PRD, and is not urgent while no code path sets the flag.",
        "Out of scope: Renaming migrations to the CLI timestamp format (finding M2).",
        "Out of scope: Establishing a process to apply migrations to a deployed environment (finding M4). That is Phase 27.",
        "Out of scope: Regenerating src/types/supabase.ts wholesale, or adding the missing cv_generation_logs types.",
        "Out of scope: Removing the type assertion in generation-logs.ts.",
        "Out of scope: Any change to storage buckets, service_role code paths, or application authorization logic.",
        "Out of scope: Any change to application behaviour, UI, or exports."
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "resume_analyses deletion behaviour is explicit",
      "description": "As a user, I want to delete my own resume analyses, so that I can remove results I no longer want kept. Direction decided 2026-08-28: add the DELETE policy. The omission in 001_initial_schema.sql is treated as an oversight - every other user-owned table in that migration grants DELETE to the owner.",
      "acceptanceCriteria": [
        "resume_analyses has a DELETE policy USING (auth.uid() = user_id).",
        "A user deleting their own analysis affects exactly 1 row.",
        "A user attempting to delete another user's analysis affects 0 rows.",
        "Existing cascade deletion from the parent resume is unchanged.",
        "FR-1: All changes land in one new forward migration. No already-applied migration file is edited.",
        "FR-2: The migration is idempotent where PostgreSQL permits it (CREATE OR REPLACE, DROP ... IF EXISTS, policy drop-then-create), so a re-run against an already-migrated database does not error.",
        "FR-3: The migration performs no destructive or lossy data change. No row is deleted and no column is dropped.",
        "FR-4: supabase/tests/rls-audit.sql is extended so that every acceptance criterion expressible as a SQL check is asserted there, and the suite passes in full.",
        "FR-5: RLS remains enabled on all eight tables and the policy count does not decrease.",
        "FR-6: No application source file changes, with the sole exception of documentation updates required by US-005.",
        "FR-7: The migration is applied and verified against the local stack only. Deploying it to any hosted environment is out of scope and requires separate explicit authorization.",
        "Regression constraint: Cross-user isolation must continue to hold on all eight tables: the 20 isolation checks in the audit harness must still pass unchanged.",
        "Regression constraint: An owner must retain SELECT, UPDATE and DELETE on their own rows wherever those policies exist today.",
        "Regression constraint: anon must continue to read 0 rows from profiles and cover_letters, and must continue to read a resume with is_public = true - this PRD does not change S3 behaviour in either direction.",
        "Regression constraint: The on_auth_user_created trigger must continue to create a profile row on signup.",
        "Regression constraint: updated_at maintenance must continue on every table that has it today.",
        "Regression constraint: pnpm build, pnpm typecheck and pnpm test must remain green, and pnpm lint must not increase above its 311-problem baseline.",
        "Verification: Migration applies cleanly - pnpm supabase db reset completes with all six migrations applied in order, from an empty database.",
        "Verification: Effective policy behaviour - the extended supabase/tests/rls-audit.sql runs against the local stack and reports every check PASS, with the new checks visible in the output. Evidence is the run output, not the migration source.",
        "Verification: Catalog state - pg_proc.proconfig for handle_new_user, pg_policy rows for resume_analyses and the five UPDATE policies, and pg_constraint for cv_generation_logs_user_id_fkey are queried directly and recorded.",
        "Verification: Idempotency - the migration is applied a second time against an already-migrated database and completes without error.",
        "Verification: Repository baseline - pnpm lint, pnpm typecheck, pnpm test and pnpm build are run and their results recorded.",
        "Verification: Documentation - docs/engineering/rls-audit.md is updated to reflect the post-migration state, including which findings are now closed.",
        "FAIL condition: Any existing check in supabase/tests/rls-audit.sql regresses from PASS.",
        "FAIL condition: Cross-user isolation weakens on any table.",
        "FAIL condition: handle_new_user loses SECURITY DEFINER, or signup stops creating a profile row.",
        "FAIL condition: The migration deletes rows, drops a column, or edits an already-applied migration file.",
        "FAIL condition: Application behaviour changes in any way observable to a user.",
        "FAIL condition: The S1 clauses are described anywhere in the commit, migration or docs as fixing a vulnerability.",
        "FAIL condition: PASS is claimed for any criterion without a recorded command and result.",
        "BLOCKER condition: cv_generation_logs contains rows whose user_id has no matching public.profiles row, making the US-004 foreign key unachievable without a data decision that this PRD does not authorize.",
        "BLOCKER condition: The local Supabase stack cannot be started, since every verification step depends on it.",
        "BLOCKER condition: Implementation is attempted on main, or on a branch other than the one approved for this work.",
        "Out of scope: The is_public column allow-list (finding S3). It is a product decision requiring its own PRD, and is not urgent while no code path sets the flag.",
        "Out of scope: Renaming migrations to the CLI timestamp format (finding M2).",
        "Out of scope: Establishing a process to apply migrations to a deployed environment (finding M4). That is Phase 27.",
        "Out of scope: Regenerating src/types/supabase.ts wholesale, or adding the missing cv_generation_logs types.",
        "Out of scope: Removing the type assertion in generation-logs.ts.",
        "Out of scope: Any change to storage buckets, service_role code paths, or application authorization logic.",
        "Out of scope: Any change to application behaviour, UI, or exports."
      ],
      "priority": 2,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "One updated_at trigger function",
      "description": "As a maintainer, I want a single updated_at trigger function, so that a change to that behaviour does not have to be made in two places.",
      "acceptanceCriteria": [
        "public.update_cover_letters_updated_at no longer exists.",
        "The cover_letters update trigger executes public.update_updated_at_column().",
        "Updating a cover_letters row still advances updated_at.",
        "Updating a row in each other table with an updated_at trigger still advances updated_at.",
        "FR-1: All changes land in one new forward migration. No already-applied migration file is edited.",
        "FR-2: The migration is idempotent where PostgreSQL permits it (CREATE OR REPLACE, DROP ... IF EXISTS, policy drop-then-create), so a re-run against an already-migrated database does not error.",
        "FR-3: The migration performs no destructive or lossy data change. No row is deleted and no column is dropped.",
        "FR-4: supabase/tests/rls-audit.sql is extended so that every acceptance criterion expressible as a SQL check is asserted there, and the suite passes in full.",
        "FR-5: RLS remains enabled on all eight tables and the policy count does not decrease.",
        "FR-6: No application source file changes, with the sole exception of documentation updates required by US-005.",
        "FR-7: The migration is applied and verified against the local stack only. Deploying it to any hosted environment is out of scope and requires separate explicit authorization.",
        "Regression constraint: Cross-user isolation must continue to hold on all eight tables: the 20 isolation checks in the audit harness must still pass unchanged.",
        "Regression constraint: An owner must retain SELECT, UPDATE and DELETE on their own rows wherever those policies exist today.",
        "Regression constraint: anon must continue to read 0 rows from profiles and cover_letters, and must continue to read a resume with is_public = true - this PRD does not change S3 behaviour in either direction.",
        "Regression constraint: The on_auth_user_created trigger must continue to create a profile row on signup.",
        "Regression constraint: updated_at maintenance must continue on every table that has it today.",
        "Regression constraint: pnpm build, pnpm typecheck and pnpm test must remain green, and pnpm lint must not increase above its 311-problem baseline.",
        "Verification: Migration applies cleanly - pnpm supabase db reset completes with all six migrations applied in order, from an empty database.",
        "Verification: Effective policy behaviour - the extended supabase/tests/rls-audit.sql runs against the local stack and reports every check PASS, with the new checks visible in the output. Evidence is the run output, not the migration source.",
        "Verification: Catalog state - pg_proc.proconfig for handle_new_user, pg_policy rows for resume_analyses and the five UPDATE policies, and pg_constraint for cv_generation_logs_user_id_fkey are queried directly and recorded.",
        "Verification: Idempotency - the migration is applied a second time against an already-migrated database and completes without error.",
        "Verification: Repository baseline - pnpm lint, pnpm typecheck, pnpm test and pnpm build are run and their results recorded.",
        "Verification: Documentation - docs/engineering/rls-audit.md is updated to reflect the post-migration state, including which findings are now closed.",
        "FAIL condition: Any existing check in supabase/tests/rls-audit.sql regresses from PASS.",
        "FAIL condition: Cross-user isolation weakens on any table.",
        "FAIL condition: handle_new_user loses SECURITY DEFINER, or signup stops creating a profile row.",
        "FAIL condition: The migration deletes rows, drops a column, or edits an already-applied migration file.",
        "FAIL condition: Application behaviour changes in any way observable to a user.",
        "FAIL condition: The S1 clauses are described anywhere in the commit, migration or docs as fixing a vulnerability.",
        "FAIL condition: PASS is claimed for any criterion without a recorded command and result.",
        "BLOCKER condition: cv_generation_logs contains rows whose user_id has no matching public.profiles row, making the US-004 foreign key unachievable without a data decision that this PRD does not authorize.",
        "BLOCKER condition: The local Supabase stack cannot be started, since every verification step depends on it.",
        "BLOCKER condition: Implementation is attempted on main, or on a branch other than the one approved for this work.",
        "Out of scope: The is_public column allow-list (finding S3). It is a product decision requiring its own PRD, and is not urgent while no code path sets the flag.",
        "Out of scope: Renaming migrations to the CLI timestamp format (finding M2).",
        "Out of scope: Establishing a process to apply migrations to a deployed environment (finding M4). That is Phase 27.",
        "Out of scope: Regenerating src/types/supabase.ts wholesale, or adding the missing cv_generation_logs types.",
        "Out of scope: Removing the type assertion in generation-logs.ts.",
        "Out of scope: Any change to storage buckets, service_role code paths, or application authorization logic.",
        "Out of scope: Any change to application behaviour, UI, or exports."
      ],
      "priority": 3,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "cv_generation_logs shares the ownership root",
      "description": "As a maintainer, I want every user-owned table to key ownership to public.profiles(id), so that joins and future policy work follow one key path.",
      "acceptanceCriteria": [
        "cv_generation_logs_user_id_fkey references public.profiles(id) with ON DELETE CASCADE.",
        "The existing SELECT and INSERT policies still restrict rows to auth.uid() = user_id - verified by execution, not by inspection.",
        "writeGenerationLog in src/lib/supabase/generation-logs.ts still inserts successfully for an authenticated user.",
        "The migration aborts with a clear error rather than silently dropping rows if any existing user_id has no matching profiles row.",
        "FR-1: All changes land in one new forward migration. No already-applied migration file is edited.",
        "FR-2: The migration is idempotent where PostgreSQL permits it (CREATE OR REPLACE, DROP ... IF EXISTS, policy drop-then-create), so a re-run against an already-migrated database does not error.",
        "FR-3: The migration performs no destructive or lossy data change. No row is deleted and no column is dropped.",
        "FR-4: supabase/tests/rls-audit.sql is extended so that every acceptance criterion expressible as a SQL check is asserted there, and the suite passes in full.",
        "FR-5: RLS remains enabled on all eight tables and the policy count does not decrease.",
        "FR-6: No application source file changes, with the sole exception of documentation updates required by US-005.",
        "FR-7: The migration is applied and verified against the local stack only. Deploying it to any hosted environment is out of scope and requires separate explicit authorization.",
        "Regression constraint: Cross-user isolation must continue to hold on all eight tables: the 20 isolation checks in the audit harness must still pass unchanged.",
        "Regression constraint: An owner must retain SELECT, UPDATE and DELETE on their own rows wherever those policies exist today.",
        "Regression constraint: anon must continue to read 0 rows from profiles and cover_letters, and must continue to read a resume with is_public = true - this PRD does not change S3 behaviour in either direction.",
        "Regression constraint: The on_auth_user_created trigger must continue to create a profile row on signup.",
        "Regression constraint: updated_at maintenance must continue on every table that has it today.",
        "Regression constraint: pnpm build, pnpm typecheck and pnpm test must remain green, and pnpm lint must not increase above its 311-problem baseline.",
        "Verification: Migration applies cleanly - pnpm supabase db reset completes with all six migrations applied in order, from an empty database.",
        "Verification: Effective policy behaviour - the extended supabase/tests/rls-audit.sql runs against the local stack and reports every check PASS, with the new checks visible in the output. Evidence is the run output, not the migration source.",
        "Verification: Catalog state - pg_proc.proconfig for handle_new_user, pg_policy rows for resume_analyses and the five UPDATE policies, and pg_constraint for cv_generation_logs_user_id_fkey are queried directly and recorded.",
        "Verification: Idempotency - the migration is applied a second time against an already-migrated database and completes without error.",
        "Verification: Repository baseline - pnpm lint, pnpm typecheck, pnpm test and pnpm build are run and their results recorded.",
        "Verification: Documentation - docs/engineering/rls-audit.md is updated to reflect the post-migration state, including which findings are now closed.",
        "FAIL condition: Any existing check in supabase/tests/rls-audit.sql regresses from PASS.",
        "FAIL condition: Cross-user isolation weakens on any table.",
        "FAIL condition: handle_new_user loses SECURITY DEFINER, or signup stops creating a profile row.",
        "FAIL condition: The migration deletes rows, drops a column, or edits an already-applied migration file.",
        "FAIL condition: Application behaviour changes in any way observable to a user.",
        "FAIL condition: The S1 clauses are described anywhere in the commit, migration or docs as fixing a vulnerability.",
        "FAIL condition: PASS is claimed for any criterion without a recorded command and result.",
        "BLOCKER condition: cv_generation_logs contains rows whose user_id has no matching public.profiles row, making the US-004 foreign key unachievable without a data decision that this PRD does not authorize.",
        "BLOCKER condition: The local Supabase stack cannot be started, since every verification step depends on it.",
        "BLOCKER condition: Implementation is attempted on main, or on a branch other than the one approved for this work.",
        "Out of scope: The is_public column allow-list (finding S3). It is a product decision requiring its own PRD, and is not urgent while no code path sets the flag.",
        "Out of scope: Renaming migrations to the CLI timestamp format (finding M2).",
        "Out of scope: Establishing a process to apply migrations to a deployed environment (finding M4). That is Phase 27.",
        "Out of scope: Regenerating src/types/supabase.ts wholesale, or adding the missing cv_generation_logs types.",
        "Out of scope: Removing the type assertion in generation-logs.ts.",
        "Out of scope: Any change to storage buckets, service_role code paths, or application authorization logic.",
        "Out of scope: Any change to application behaviour, UI, or exports."
      ],
      "priority": 4,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "UPDATE policies state their check expression explicitly",
      "description": "As a maintainer, I want every UPDATE policy to declare WITH CHECK explicitly, so that a reader does not have to know PostgreSQL's substitution rule to determine whether ownership transfer is possible.",
      "acceptanceCriteria": [
        "profiles, resumes, job_applications, career_goals and ai_suggestions each have a non-null polwithcheck for their UPDATE policy.",
        "Ownership reassignment remains refused on all six tables, unchanged from the audit baseline.",
        "The migration comment states that this is a readability change and closes no vulnerability.",
        "The withdrawn-S1 section of rls-audit.md is updated to record that the clauses now exist, without reinstating the finding.",
        "FR-1: All changes land in one new forward migration. No already-applied migration file is edited.",
        "FR-2: The migration is idempotent where PostgreSQL permits it (CREATE OR REPLACE, DROP ... IF EXISTS, policy drop-then-create), so a re-run against an already-migrated database does not error.",
        "FR-3: The migration performs no destructive or lossy data change. No row is deleted and no column is dropped.",
        "FR-4: supabase/tests/rls-audit.sql is extended so that every acceptance criterion expressible as a SQL check is asserted there, and the suite passes in full.",
        "FR-5: RLS remains enabled on all eight tables and the policy count does not decrease.",
        "FR-6: No application source file changes, with the sole exception of documentation updates required by US-005.",
        "FR-7: The migration is applied and verified against the local stack only. Deploying it to any hosted environment is out of scope and requires separate explicit authorization.",
        "Regression constraint: Cross-user isolation must continue to hold on all eight tables: the 20 isolation checks in the audit harness must still pass unchanged.",
        "Regression constraint: An owner must retain SELECT, UPDATE and DELETE on their own rows wherever those policies exist today.",
        "Regression constraint: anon must continue to read 0 rows from profiles and cover_letters, and must continue to read a resume with is_public = true - this PRD does not change S3 behaviour in either direction.",
        "Regression constraint: The on_auth_user_created trigger must continue to create a profile row on signup.",
        "Regression constraint: updated_at maintenance must continue on every table that has it today.",
        "Regression constraint: pnpm build, pnpm typecheck and pnpm test must remain green, and pnpm lint must not increase above its 311-problem baseline.",
        "Verification: Migration applies cleanly - pnpm supabase db reset completes with all six migrations applied in order, from an empty database.",
        "Verification: Effective policy behaviour - the extended supabase/tests/rls-audit.sql runs against the local stack and reports every check PASS, with the new checks visible in the output. Evidence is the run output, not the migration source.",
        "Verification: Catalog state - pg_proc.proconfig for handle_new_user, pg_policy rows for resume_analyses and the five UPDATE policies, and pg_constraint for cv_generation_logs_user_id_fkey are queried directly and recorded.",
        "Verification: Idempotency - the migration is applied a second time against an already-migrated database and completes without error.",
        "Verification: Repository baseline - pnpm lint, pnpm typecheck, pnpm test and pnpm build are run and their results recorded.",
        "Verification: Documentation - docs/engineering/rls-audit.md is updated to reflect the post-migration state, including which findings are now closed.",
        "FAIL condition: Any existing check in supabase/tests/rls-audit.sql regresses from PASS.",
        "FAIL condition: Cross-user isolation weakens on any table.",
        "FAIL condition: handle_new_user loses SECURITY DEFINER, or signup stops creating a profile row.",
        "FAIL condition: The migration deletes rows, drops a column, or edits an already-applied migration file.",
        "FAIL condition: Application behaviour changes in any way observable to a user.",
        "FAIL condition: The S1 clauses are described anywhere in the commit, migration or docs as fixing a vulnerability.",
        "FAIL condition: PASS is claimed for any criterion without a recorded command and result.",
        "BLOCKER condition: cv_generation_logs contains rows whose user_id has no matching public.profiles row, making the US-004 foreign key unachievable without a data decision that this PRD does not authorize.",
        "BLOCKER condition: The local Supabase stack cannot be started, since every verification step depends on it.",
        "BLOCKER condition: Implementation is attempted on main, or on a branch other than the one approved for this work.",
        "Out of scope: The is_public column allow-list (finding S3). It is a product decision requiring its own PRD, and is not urgent while no code path sets the flag.",
        "Out of scope: Renaming migrations to the CLI timestamp format (finding M2).",
        "Out of scope: Establishing a process to apply migrations to a deployed environment (finding M4). That is Phase 27.",
        "Out of scope: Regenerating src/types/supabase.ts wholesale, or adding the missing cv_generation_logs types.",
        "Out of scope: Removing the type assertion in generation-logs.ts.",
        "Out of scope: Any change to storage buckets, service_role code paths, or application authorization logic.",
        "Out of scope: Any change to application behaviour, UI, or exports."
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    }
  ]
}
